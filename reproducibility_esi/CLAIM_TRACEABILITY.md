# Claim traceability for Earth Science Informatics

This matrix separates the confirmatory action-allocation claim from the earlier degradation-inference evidence. Paths are relative to the package root unless stated otherwise.

## Status

**CLAIMS TRACEABLE.** The capacity-equivalent claim is mapped to its script and bootstrap artifacts, is labeled secondary/post-confirmation throughout the submission package, and is explicitly bounded to retraining count rather than economic or compute savings.

| Submission-facing claim | Evidence artifact | Regeneration or validation route | Allowed wording / boundary |
|---|---|---|---|
| The benchmark contains 330 source–target–model decisions and 990 realized actions per evaluation period. | `realized_action_confirmatory_predictions_outcomes.csv`; three `realized_actions_confirm_*_s1.csv` files | `scripts/evaluate_realized_action_confirmatory.py` rejects any count other than 990, duplicate key, incomplete triplet, or mismatch with the 330 frozen decisions. | Descriptive benchmark claim only; the 330 decisions are not independent inferential units. |
| The frozen action-value allocation has confirmatory mean budget-curve AUC 2.547325 versus 2.550777 for random expected allocation. | `realized_action_confirmatory_budget_curve.csv`; `realized_action_confirmatory_target_auc.csv`; `realized_action_confirmatory_report.md` | `scripts/evaluate_realized_action_confirmatory.py`, functions `make_curves` and `target_auc`. | The metric is discrete target-level AUC over budgets 0–30; lower is better. |
| The policy improves 10/11 targets, with one-sided paired Wilcoxon p=0.000977 (two-sided robustness p=0.001953). | `realized_action_confirmatory_target_auc.csv`; confirmatory report | Paired test is computed over 11 target AUCs by the evaluation script, which emits both the prespecified one-sided p-value and a `wilcoxon_two_sided_p` robustness column. | Random is the single primary comparison; do not treat 330 decisions as independent replicates. The one-sided test is the prespecified primary analysis; the two-sided value is a post-confirmation robustness check that does not enter the success rule or the Holm family. |
| The policy closes 43.5% of the random-to-restricted-oracle gap. | Confirmatory target AUC and report | `(AUC_random - AUC_action-value) / (AUC_random - AUC_oracle)` from the regenerated report. | Report together with the absolute gain of 0.003452 MAE-AUC and call the oracle restricted. |
| Mean action-value minus random AUC is favorable in all three forecasting families. | `realized_action_confirmatory_predictions_outcomes.csv`; confirmatory report, family diagnostic table | Family-stratified curves and differences generated by the evaluation script. | Do not claim improvement in every target-within-family cell. |
| All five shift-only rankings trail action-value after Holm correction. | Confirmatory report, secondary comparison table | One-sided paired Wilcoxon tests over targets; Holm adjustment is applied only to the five shift-only baselines. | Keep these comparisons secondary; do not include random in the Holm family. |
| Deploy, adapt, and retrain outcomes are heterogeneous; best action counts are 40/48/242. | Merged confirmatory predictions/outcomes; confirmatory report | Row-wise minimum over the three realized action MAEs. | Supports a decision problem, but also shows retraining dominance for the neural families. |
| The unrestricted learned policy does not beat always retrain. | `realized_action_budget_dev11_*`; development diagnostics in the supplement | Frozen development-policy analysis. | Mandatory negative result; no claim of superiority when retraining is unlimited. |
| Physical descriptors add degradation-inference value for the spatially structured families. | Earlier physical/shift benchmark artifacts and supplementary Tables/Figures S7, S1, S2 | Earlier benchmark scripts and hierarchical-bootstrap audit retained outside the primary confirmation. | Secondary evidence-layer result only; not a confirmatory action outcome and not universal across all six families. |
| The full confirmatory curve has a capacity-equivalent gain of 4.03 random retrainings (target-cluster bootstrap 95% CI 3.18--5.17); at budget 15, random requires 21.51 retrainings to match PhyK-TAS at 15. | `capacity_equivalent_result.json`; `capacity_equivalent_selected_budgets.csv`; `capacity_equivalent_target_summary.csv`; `capacity_equivalent_report.md` | `scripts/analyze_capacity_equivalent_gain.py` validates the complete 11-target, 0--30 grid, endpoint equality, exact linearity of random expected allocation, and positive marginal random improvement; it uses 100,000 target-cluster bootstrap replicates with seed 20260822 and leave-one-target-out sensitivity. | Secondary post-confirmation practical-significance analysis. Say “capacity-equivalent retraining count,” not actual compute, energy, time, monetary, engineering, or economic savings. Do not call it a new confirmatory endpoint or claim the same saving in every region. |
| A modern learner benchmark changes the development ranking but yields only small post-hoc external differences; ExtraTrees is best externally and TabDPT v1.2 is the strongest deep challenger. | `deep_policy_benchmark_dev_summary.csv`; `deep_policy_benchmark_external5_summary.csv`; `deep_policy_benchmark_external5_comparisons.csv`; `classical_policy_benchmark_dev_summary.csv`; `classical_policy_benchmark_dev_target_auc.csv`; `deep_policy_benchmark_report.md` | `scripts/benchmark_deep_policy_learners.py` and `scripts/benchmark_classical_policy_logo.py`; the scripts reject incomplete held-target prediction grids, missing scores, or duplicate decision keys and regenerate budget curves and target AUCs. | Model-selection/robustness analysis only. The five-region comparison is post hoc for the newly added DL learners and must not be called independent confirmation or used to overwrite the frozen Ridge claim. |

Determinism check on 22 August 2026: rerunning `scripts/evaluate_realized_action_confirmatory.py` reproduced byte-identical SHA-256 hashes for the merged table, budget curve, target AUC table, and confirmatory report.

Determinism re-check on 23 August 2026 after adding the two-sided robustness column: the merged table, budget curve, and target AUC table remain byte-identical; only the report gained the new column.

## Released archive

The `reproducibility_esi/` directory of this repository carries every artifact and script named above, the frozen protocols, this matrix, and a SHA-256 manifest. On 2 September 2026, all manifest entries verified; rerunning `evaluate_realized_action_confirmatory.py` and `analyze_capacity_equivalent_gain.py` reproduced the reported primary and capacity-equivalent results.
